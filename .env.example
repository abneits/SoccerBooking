DATABASE_URL=postgresql://user:password@<prod-db-host>:5432/soccerbooking
SECRET_KEY=change-me-in-production
WEBHOOK_URL=https://your-webhook-url
TIMEZONE=Europe/Paris
SESSION_MAX_AGE=604800
DB_HOST=<prod-db-host>
DB_USER=user

# Set to true ONLY for running the test suite — exposes /internal/set-time endpoint
# NEVER set in production
# TESTING=false
