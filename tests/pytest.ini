[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = api ui
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --color=yes -v --tb=short --no-header -p no:warnings --timeout=30
